ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional — override the default model (claude-opus-4-7).
# If you want to run cheaper iterations while tuning the harness, set:
#   COLD_CALL_MODEL=claude-sonnet-4-6
# Note: switching models invalidates comparisons against prior baselines.
# COLD_CALL_MODEL=claude-opus-4-7
