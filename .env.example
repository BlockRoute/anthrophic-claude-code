# Cloud mode (optional). Without these the app runs fully on-device (local mode).
# Create a project at https://supabase.com, run supabase/schema.sql, then paste:
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
